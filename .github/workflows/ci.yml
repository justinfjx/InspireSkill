name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}-${{ github.sha }}
  cancel-in-progress: true

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    defaults:
      run:
        working-directory: cli

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: cli/uv.lock

      - name: Install dependencies
        run: uv sync --dev --locked

      - name: Ruff
        run: uv run ruff check inspire tests

      - name: mypy
        run: uv run mypy

      - name: Unit tests
        run: >
          uv run python -c 'import faulthandler, os, pytest, sys;
          faulthandler.dump_traceback_later(240, file=sys.stderr, exit=True);
          code = pytest.main(["-q"]); sys.stdout.flush(); sys.stderr.flush();
          os._exit(code)'

      - name: Build package
        run: uv build
